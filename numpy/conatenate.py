import numpy as np
# a = np.array([3, 4, 2, 6])
# a = np.append(a,100)
# b = np.array([8, 6, 5, 3])
# c = np.concatenate((a, b))
# print(c)

g=np.array([[1,2],[3,4]])
g=np.append(g,[[10],[20]],axis=1)
print(g)